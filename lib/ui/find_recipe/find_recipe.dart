import 'package:flutter/material.dart';

class FindRecipe extends StatelessWidget {
  const FindRecipe({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        child: Text('Find Recipes'),
      ),
    );
  }
}
